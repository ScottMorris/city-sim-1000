{
  "name": "city-sim-1000-workspace",
  "private": true,
  "scripts": {
    "dev":                  "npm --prefix app run dev",
    "build":                "npm --prefix app run build",
    "preview":              "npm --prefix app run preview",
    "lint":                 "npm --prefix app run lint",
    "test":                 "npm --prefix app run test",
    "build:favicon":        "npm --prefix app run build:favicon",
    "build:radio-playlist": "npm --prefix app run build:radio-playlist"
  },
  "engines": {
    "node": ">=20"
  }
}
