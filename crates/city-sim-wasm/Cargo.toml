[package]
name        = "city-sim-wasm"
description = "WASM cdylib wrapper around city-sim-core for the browser Worker target"
version.workspace    = true
edition.workspace    = true
authors.workspace    = true
license.workspace    = true

[lib]
# Keep lib name as sim_wasm so wasm-pack output files (sim_wasm.js etc.) are stable.
name       = "sim_wasm"
crate-type = ["cdylib"]

[dependencies]
sim_core     = { path = "../city-sim-core",     package = "city-sim-core"     }
sim_protocol = { path = "../city-sim-protocol", package = "city-sim-protocol" }
serde        = { workspace = true }
postcard     = { workspace = true }
wasm-bindgen = { workspace = true }
js-sys       = { workspace = true }
